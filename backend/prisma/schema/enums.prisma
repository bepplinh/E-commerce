// ──────────────────────────────────────────────
//  ENUMS — Shared enums across all modules
// ──────────────────────────────────────────────

enum UserStatus {
  ACTIVE
  INACTIVE
  BANNED
}

enum DiscountType {
  PERCENTAGE
  FIXED
}

enum OrderStatus {
  PENDING
  CONFIRMED
  PROCESSING
  SHIPPED
  DELIVERED
  CANCELED
  REFUNDED
}

enum PaymentMethod {
  COD
  BANK_TRANSFER
  MOMO
  VNPAY
  ZALOPAY
  CREDIT_CARD
}

enum PaymentStatus {
  PENDING
  PAID
  FAILED
  REFUNDED
}

enum ShippingStatus {
  PREPARING
  PICKED_UP
  IN_TRANSIT
  DELIVERED
  RETURNED
}

enum NotificationType {
  ORDER
  PROMOTION
  SYSTEM
  REVIEW
}
